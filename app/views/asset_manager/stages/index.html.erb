<% title 'List of Stages' %>

<table>
  <tr>
  </tr>

<% @stages.each do |stage| %>
  <tr>
    <td><%=h stage.id %></td>
    <td><%=h stage.title %></td>
    <td><%= link_to 'Show', stageasset_stage_path(stage) %></td>
    <td><%= link_to 'Edit', edit_stage_path(stage) %></td>
    <td><%= link_to 'Destroy', stage, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />
<%= will_paginate @stages %>
<%= link_to 'New stage', new_stage_path %>